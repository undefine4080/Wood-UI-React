import { describe } from 'node:test';
import renderer from 'react-test-renderer';
import Button from "../Button";

// it( 'disable the button when the disabled attribute is true', () => {
//     const component = renderer.create(
//         <Button onClick={ () => console.log( 'click' ) } disabled>test</Button>
//     );
// } );
describe( 'hello ', () => {
    test( '3+2=5', () => {
        expect( 3 + 2 ).toBe( 5 );
    } );
} );