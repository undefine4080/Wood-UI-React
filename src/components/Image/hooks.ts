function useTransform() {
    const scaleDown = () => {};
    const scaleUp = () => {};
    const reset = () => {};
    const full = () => {};
    const rotate = () => {};

    return { scaleDown, scaleUp, reset, full, rotate };
}

export { useTransform };
